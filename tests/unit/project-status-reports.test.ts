import{describe,expect,it}from"vitest";import{createStatusReportSchema,publishStatusReportSchema,updateStatusReportSchema}from"../../features/project-status-reports/schemas";
const base={projectId:"11111111-1111-4111-8111-111111111111",reportingPeriodStart:"2026-01-01",reportingPeriodEnd:"2026-02-01",overallHealth:"Green",scopeHealth:"Amber",scheduleHealth:"Red",budgetHealth:"Not Assessed",resourceHealth:"Green",riskHealth:"Amber",executiveSummary:"Summary",accomplishments:"Done",plannedWork:"Next",concerns:" ",decisionsRequired:"None",supportRequired:""};
describe("Status Report validation",()=>{it("normalizes optional narratives",()=>{const x=createStatusReportSchema.parse(base);expect(x.concerns).toBeNull();expect(x.supportRequired).toBeNull()});it("accepts the 4000 character boundary",()=>expect(createStatusReportSchema.safeParse({...base,executiveSummary:"x".repeat(4000)}).success).toBe(true));it("rejects blank required narratives",()=>expect(createStatusReportSchema.safeParse({...base,executiveSummary:" "}).success).toBe(false));it("rejects more than 4000 characters",()=>expect(createStatusReportSchema.safeParse({...base,plannedWork:"x".repeat(4001)}).success).toBe(false));it("rejects reversed dates",()=>expect(createStatusReportSchema.safeParse({...base,reportingPeriodStart:"2026-03-01"}).success).toBe(false));it("rejects future period end",()=>expect(createStatusReportSchema.safeParse({...base,reportingPeriodEnd:"2099-01-01"}).success).toBe(false));it("rejects invalid health",()=>expect(createStatusReportSchema.safeParse({...base,riskHealth:"Blue"}).success).toBe(false));it("rejects trusted field injection",()=>expect(createStatusReportSchema.safeParse({...base,status:"Published"}).success).toBe(false));it("requires a report UUID for update",()=>expect(updateStatusReportSchema.safeParse({...base,reportId:"bad"}).success).toBe(false));it("requires explicit publication confirmation",()=>expect(publishStatusReportSchema.safeParse({projectId:base.projectId,reportId:base.projectId,confirmPublication:""}).success).toBe(false));it("accepts explicit publication confirmation",()=>expect(publishStatusReportSchema.safeParse({projectId:base.projectId,reportId:base.projectId,confirmPublication:"confirmed"}).success).toBe(true));});
