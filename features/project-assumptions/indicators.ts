export type AssumptionStatus="Active"|"Under Validation"|"Validated"|"Invalidated"|"Retired";export type Confidence="Low"|"Medium"|"High";export function getAssumptionScheduleLabel(d:string,s:AssumptionStatus,t:string){if(["Validated","Invalidated","Retired"].includes(s))return s;if(d<t)return"Validation overdue";if(d===t)return"Validation due today";return(Date.parse(`${d}T00:00:00Z`)-Date.parse(`${t}T00:00:00Z`))/86400000<=14?"Validation due soon":"Scheduled validation"}export function getAssumptionAttentionReasons(d:string,s:AssumptionStatus,c:Confidence,eligible:boolean,t:string){if(["Validated","Invalidated","Retired"].includes(s))return[];const r:string[]=[];if(!eligible)r.push("Inactive owner");if(d<t)r.push("Validation overdue");else if(d===t)r.push("Validation due today");if(s==="Under Validation")r.push("Under validation");if(c==="Low")r.push("Low confidence");return r}
