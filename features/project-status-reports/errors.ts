export function getSafeStatusReportError(error:{code?:string;message?:string}){if(error.code==="23505")return "A status report already exists for this reporting period.";if(error.code==="23514"||error.code==="22023")return "The report contains invalid information. Review the reporting period and required fields.";if(error.code==="42501")return "This status report operation is not permitted.";return "The status report could not be saved. Try again.";}
