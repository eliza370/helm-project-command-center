import { z } from "zod";
const dateOnly=z.string().regex(/^\d{4}-\d{2}-\d{2}$/,"Enter a valid due date.").refine(value=>{const date=new Date(`${value}T00:00:00Z`);return !Number.isNaN(date.valueOf())&&date.toISOString().slice(0,10)===value},"Enter a valid due date.");
const optionalUuid=z.union([z.literal(""),z.uuid()]).transform(value=>value||undefined);
export const deliverableInputSchema=z.object({operation:z.enum(["create","update"]),projectId:z.uuid(),deliverableId:z.uuid().optional(),title:z.string().trim().min(1,"Enter a deliverable title.").max(200,"Use 200 characters or fewer."),description:z.string().trim().max(4000,"Use 4,000 characters or fewer.").transform(v=>v||undefined),acceptanceCriteria:z.string().trim().min(1,"Enter acceptance criteria.").max(4000,"Use 4,000 characters or fewer."),ownerMembershipId:z.uuid("Select an eligible owner."),milestoneId:optionalUuid,dueDate:dateOnly}).strict().superRefine((value,context)=>{if(value.operation==="update"&&!value.deliverableId)context.addIssue({code:"custom",path:["deliverableId"],message:"Select a deliverable to edit."})});
export const deliverableTransitionSchema=z.object({projectId:z.uuid(),deliverableId:z.uuid(),operation:z.enum(["start","submit","accept","return","cancel"]),confirm:z.string().optional(),reviewFeedback:z.string().trim().max(4000,"Use 4,000 characters or fewer.").optional()}).strict().superRefine((value,context)=>{if((value.operation==="accept"||value.operation==="cancel")&&value.confirm!=="confirmed")context.addIssue({code:"custom",path:["confirm"],message:"Confirm this action."});if(value.operation==="return"&&!value.reviewFeedback)context.addIssue({code:"custom",path:["reviewFeedback"],message:"Enter review feedback."})});

