import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/lib/supabase/database.types";
const select="*,creator:profiles!project_change_requests_created_by_fkey(full_name),editor:profiles!project_change_requests_last_edited_by_fkey(full_name),submitter:profiles!project_change_requests_submitted_by_fkey(full_name),decider:profiles!project_change_requests_decided_by_fkey(full_name),withdrawer:profiles!project_change_requests_withdrawn_by_fkey(full_name),plan_editor:profiles!project_change_requests_implementation_plan_updated_by_fkey(full_name),starter:profiles!project_change_requests_implementation_started_by_fkey(full_name),outcome_actor:profiles!project_change_requests_outcome_recorded_by_fkey(full_name),implementation_owner:project_members!project_change_requests_implementation_owner_membership_id_fkey(access_level,profile:profiles!project_members_user_id_fkey(full_name))";
export const getProjectChanges=(s:SupabaseClient<Database>,id:string)=>s.from("project_change_requests").select(select).eq("project_id",id).order("requested_date",{ascending:false}).order("id");export const getProjectChange=(s:SupabaseClient<Database>,projectId:string,id:string)=>s.from("project_change_requests").select(select).eq("project_id",projectId).eq("id",id).maybeSingle();export const getEligibleChangeOwners=(s:SupabaseClient<Database>,id:string)=>s.rpc("get_eligible_change_owners",{p_project_id:id});
